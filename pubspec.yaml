dependencies:
  flutter:
    uses-material-design: true
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Core Logic & Storage
  shared_preferences: ^2.2.3
  path_provider: ^2.1.3
  intl: ^0.19.0
  http: ^1.6.0
  
  # PDF & Printing (Numerology Reports)
  pdf: ^3.10.1
  printing: ^5.10.0
  
  # Sharing & Screenshots (Grid Sharing)
  screenshot: ^3.0.0
  share_plus: ^12.0.1
  
  # Localization
  flutter_localizations:
    sdk: flutter